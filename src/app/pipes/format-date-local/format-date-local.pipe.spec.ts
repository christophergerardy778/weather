import { FormatDateLocalPipe } from './format-date-local.pipe';

describe('FormatDateLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
